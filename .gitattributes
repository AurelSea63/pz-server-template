# Line-ending normalization: LF everywhere (Linux/Docker repo)
* text=auto eol=lf

# Shell scripts MUST be LF (otherwise "bad interpreter" in the container)
*.sh text eol=lf
*.lua text eol=lf

# Binary files
*.png binary
*.jpg binary
*.tar.gz binary
*.zip binary
